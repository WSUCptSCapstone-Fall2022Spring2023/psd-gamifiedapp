threatsPresent = True

threats = [5,1,6,8,2,4,6,7,2,67,5,154,5,5,51,5,65,7527,725,2,4]
errorCode = 0

def GetTarget():
	global threats
	global errorCode
	
	if len(threats) == 0:
		errorCode = 2
		return 0
	else:
		return threats[0]

def SetTarget(number):
	global threatsPresent
	global errorCode
	global threats
	
	if len(threats) > 0:
		if threats[0] != number:
			errorCode = 1
			return
		threats = threats[1 : ]
		if len(threats) == 0:
			threatsPresent = False
			parent.push_user_scope()

def simulate():
	global errorCode
	global threats

	try:
    		parent.execute_user_code()
	except Exception as e:
		parent.end_simulation(-1, repr(e))
		return 

	if errorCode == 2:
		parent.end_simulation(3)
	elif errorCode == 1:
		parent.end_simulation(2)
	elif len(threats) != 0:
		parent.end_simulation(1)
	else:
		parent.end_simulation(0)

def simulate_tick():
	return 0